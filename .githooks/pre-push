#!/usr/bin/env bash
set -euo pipefail

if [[ "${SKIP_PRE_PUSH_VERIFY:-0}" == "1" ]]; then
  exit 0
fi

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

./scripts/verify-push.sh
